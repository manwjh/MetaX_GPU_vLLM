# 发版验收报告：`muxi_gemma4_26B_TP1`（2026-04-17）

## 1) 结果总览（带数据）

| 检查项 | 实测数据 | 结论 | 证据文件 |
|---|---|---|---|
| 包内静态检查 | `verify_release_package: OK` | PASS | `verification/evidence/01_package_static_verify.log` |
| Monorepo 基线检查 | `gemma4_client_parse: ok`；`verify_metax_gemma4_patch_anchors: ok` | PASS | `verification/evidence/00_monorepo_make_check.log` |
| 现场服务健康 | `health_HTTP 200`；`health_latency_ms 4.48`；`health_model_count 1` | PASS | `verification/evidence/02_changsha_field_excerpt.txt` |
| 现场中文矩阵 | `MATRIX_ACCEPT_STATUS PASS`；`MATRIX_BEST_CJK_RATIO 0.941176`；`MATRIX_ELAPSED_SEC 11.888` | PASS | `verification/evidence/02_changsha_field_excerpt.txt` |
| 版本与模型识别 | `vllm 0.15.0`；`transformers 5.5.0`；`config_model_type gemma4` | PASS | `verification/evidence/02_changsha_field_excerpt.txt` |
| 服务器当前运行态快照 | `VLLM_GEMMA4_METAX_CHAT_PROMPT_ALIGN=0`；`VLLM_ATTENTION_BACKEND=TRITON_ATTN`；`CUDA_VISIBLE_DEVICES=6` | PASS | `verification/evidence/03_server_runtime_snapshot_2026-04-17.log` |
| 包内 TUI 探针（你好） | `http_status: 200` 且返回可读中文 | PASS | `verification/evidence/04_app_probe_nihao_2026-04-17.txt` |
| 包内 TUI 探针（数学+流式） | `http_status: 200` 且流式可读输出 | PASS | `verification/evidence/05_app_probe_math_stream_2026-04-17.txt` |

## 2) 关键原始输出（节选）

### 2.1 包内静态检查

```text
== verify_release_package root=/Users/wangjunhui/playcode/changsha_gpu/dist/muxi_gemma4_26B_TP1 ==
verify_release_package: OK
```

### 2.2 Monorepo 基线检查

```text
gemma4_client_parse: ok
verify_metax_gemma4_patch_anchors: ok
```

### 2.3 长沙现场同源栈检查（发版轮次摘录）

```text
health_HTTP 200
health_latency_ms 4.48
health_model 0 gemma-4-26B-A4B-it

MATRIX_BEST_CJK_RATIO 0.941176
MATRIX_ACCEPT_STATUS PASS
MATRIX_ELAPSED_SEC 11.888

Name: vllm
Version: 0.15.0
Name: transformers
Version: 5.5.0
config_model_type gemma4
```

## 3) 执行说明

- 本报告中的命令输出来自本包同批次证据文件，文件名固定在 `verification/evidence/`。
- 其中现场数据为 `changsha_autoverify` 发版轮次关键行摘录（非人工口述）。
- 服务器侧当前生效参数以 `03_server_runtime_snapshot_2026-04-17.log` 为准（含 `docker top` 启动参数与 `ENGINE_ENV_PROBE` 环境变量）。

## 4) 复现路径（接收方）

1. 解压本包后执行：`bash scripts/verify_release_package.sh`。
2. 按 `README.md` + `docs/QUICKSTART.md` 启动服务。
3. 容器内执行：`scripts/verify_service.sh`。

## 5) 发布结论

在“使用本包 `Dockerfile` 构建镜像（含 `transformers==5.5.0`）”前提下，本版发布包具备可审计的补丁、安装路径、以及带数据的验证证据，结论为 **可发布**。
